using System;
using System.Collections.Generic;
using UnityEngine;

public static class MDs
{
    public enum State { Getting, Losing, Staying };
    public const int UnsetForPositive = -1;
    public enum LightState {Light, Dark};
}
